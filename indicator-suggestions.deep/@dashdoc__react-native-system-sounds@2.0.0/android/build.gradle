// DEFANGED: static-analysis cache — do not execute
throw new Exception('DEFANGED: static-analysis cache — do not execute')
plugins {
  id 'com.android.library'
  id 'expo-module-gradle-plugin'
}

group = 'expo.modules.rnsystemsounds'
version = '2.0.0'

android {
  namespace "expo.modules.rnsystemsounds"

  defaultConfig {
    versionCode 1
    versionName "2.0.0"
  }

  lintOptions {
    abortOnError false
  }
}
