{
  "name": "@logicpearl/try",
  "version": "0.1.1",
  "description": "Kick the tires on LogicPearl. One command, one deterministic verdict, zero setup. See how a compiled rule artifact compares to an LLM.",
  "keywords": ["logicpearl", "rules", "wasm", "agent", "mcp", "deterministic", "llm"],
  "license": "MIT",
  "type": "module",
  "bin": {
    "logicpearl-try": "dist/cli.js"
  },
  "files": [
    "dist",
    "artifacts/**/*.json",
    "artifacts/**/*.wasm",
    "captures/*.json",
    "captures/*.md",
    "captures/*.txt",
    "README.md"
  ],
  "scripts": {
    "build": "tsc",
    "prepare": "tsc",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@logicpearl/browser": "^0.1.4",
    "mri": "^1.2.0",
    "picocolors": "^1.0.0"
  },
  "optionalDependencies": {
    "@anthropic-ai/sdk": "^0.20.0",
    "openai": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/LogicPearlHQ/logicpearl.git",
    "directory": "packages/logicpearl-try"
  },
  "homepage": "https://logicpearl.com/quickstart"
}
