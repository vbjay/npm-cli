{
  "name": "anvil-sol",
  "version": "0.7.0",
  "description": "Anvil — Anchor → Pinocchio/Native transpiler with a byte-equal proof gate. Converts Anchor source into a cargo-buildable project from a single typed IR (deterministic emit + validator + portability lint), then proves the port: anvil verify builds both binaries and byte-compares on-chain state under LiteSVM.",
  "type": "module",
  "license": "Apache-2.0",
  "homepage": "https://anvilsol.xyz",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Pratikkale26/Anvil.git",
    "directory": "cli"
  },
  "bugs": {
    "url": "https://github.com/Pratikkale26/Anvil/issues"
  },
  "keywords": [
    "solana",
    "anchor",
    "pinocchio",
    "transpiler",
    "compute-units",
    "cu-optimization",
    "rust",
    "cli"
  ],
  "author": "Pratik Kale",
  "bin": {
    "anvil": "./src/anvil.js"
  },
  "files": [
    "src",
    "migrate",
    "README.md"
  ],
  "engines": {
    "node": ">=20.19.0 <21 || >=22.12.0"
  },
  "peerDependencies": {
    "litesvm": "^0.7.0",
    "@solana/web3.js": "^1.98.0",
    "@noble/hashes": "^2.0.0"
  },
  "peerDependenciesMeta": {
    "litesvm": { "optional": true },
    "@solana/web3.js": { "optional": true },
    "@noble/hashes": { "optional": true }
  },
  "dependencies": {
    "tree-sitter-rust": "^0.24.0",
    "web-tree-sitter": "^0.26.7",
    "zod": "^4.3.6"
  },
  "scripts": {
    "test": "bun anvil.ts --help",
    "prepack": "bun scripts/prepack.ts",
    "prepack:check": "bun scripts/prepack-check.ts",
    "smoke": "bun pack && cd /tmp && rm -rf _anvil-sol-smoke && mkdir _anvil-sol-smoke && cd _anvil-sol-smoke && bun add $OLDPWD/anvil-sol-*.tgz && bunx anvil-sol --version"
  }
}
